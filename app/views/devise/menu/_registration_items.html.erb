<% if user_signed_in? %>
  <div class="row">
  <%= link_to "Edit registration", edit_user_registration_path, class: "btn btn-small btn-primary" %>
  <br/></div>
<% else %>
  <div class="row">
  <%= link_to"Sign Up Now", new_user_registration_path,class: "btn btn-small btn-primary"   %>
  <br/></div>
<% end %>